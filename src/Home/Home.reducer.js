import { options } from '../Utils/Options';

const initialState = {
    colorScheme: options
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
        return state
    }
}